__pycache__/
*.pyc
*.pyo
**/__pycache__/
.venv/
venv/
env/
*.egg-info/

# Deps are managed via requirements.txt for now (see backend/Dockerfile's
# comment) - this is a local-only lockfile from testing, not an adopted
# uv project-mode lock.
backend/uv.lock

# local secrets — never commit
backend/.env
.env

# local POC storage — real client documents/templates never go into git
/clients/*
!/clients/.gitkeep
/templates/*
!/templates/.gitkeep

# node
frontend/node_modules/
frontend/dist/
node_modules/
dist/
build/

# logs
*.log

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
