# Managed by ansible — do not edit by hand.
# Rendered from deploy/ansible/roles/app_deploy/templates/env.j2 on each
# deploy. Change the source template or the ansible variables, then re-run
# the playbook.

DATABASE_URL={{ database_url }}
OPENAI_API_KEY={{ openai_api_key }}
SUPABASE_URL={{ supabase_url }}
SUPABASE_KEY={{ supabase_key }}
LOG_LEVEL={{ log_level | default('INFO') }}
ENVIRONMENT={{ environment_name }}
CORS_ALLOWED_ORIGINS={{ cors_allowed_origins | default('') }}
IFRAME_ALLOWED_ORIGINS={{ iframe_allowed_origins | default('') }}

# NOTE: storage_backend defaults to "local" until STORAGE_BACKEND=sharepoint
# and the SHAREPOINT_* vars below are set — none of them are wired into this
# template yet. Add them here (with real values coming from ansible-vault,
# same as openai_api_key/supabase_key above) once SharePoint hosting is
# actually being deployed, not before:
# STORAGE_BACKEND={{ storage_backend | default('local') }}
# SHAREPOINT_TENANT_ID={{ sharepoint_tenant_id | default('') }}
# SHAREPOINT_CLIENT_ID={{ sharepoint_client_id | default('') }}
# SHAREPOINT_CLIENT_SECRET={{ sharepoint_client_secret | default('') }}
# SHAREPOINT_SITE_ID={{ sharepoint_site_id | default('') }}
# SHAREPOINT_DRIVE_ID={{ sharepoint_drive_id | default('') }}
# SHAREPOINT_ROOT_PATH={{ sharepoint_root_path | default('') }}
